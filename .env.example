# SwimBuddz Backend – Example Environment Variables
# Copy this file to `.env` and fill in real values for local development or deployment.

# Primary Postgres connection string.
# Use the async driver for SQLAlchemy by default.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/swimbuddz

# Supabase
SUPABASE_URL=replace-with-supabase-url
SUPABASE_PROJECT_ID=replace-with-supabase-project-id
SUPABASE_ANON_KEY=replace-with-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key
SUPABASE_JWT_SECRET=local-supabase-jwt-secret

# Optional: log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
ENVIRONMENT=replace-with-environment

# => Media Service Environment Variables

# Storage backend (supabase or s3)
STORAGE_BACKEND=supabase

# Supabase Storage (recommended - default)
SUPABASE_STORAGE_BUCKET=swimbuddz-media

# AWS S3 (alternative - if STORAGE_BACKEND=s3)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key  
# AWS_S3_BUCKET=your_bucket_name
# AWS_REGION=us-east-1

# Placeholders for future payment integration (Paystack/Flutterwave).
PAYSTACK_SECRET_KEY=your-paystack-secret-key
PAYSTACK_PUBLIC_KEY=your-paystack-public-key
PAYSTACK_CALLBACK_URL=http://localhost:3000/dashboard/billing
FLUTTERWAVE_SECRET_KEY=your-flutterwave-secret-key

# Pricing (NGN)
COMMUNITY_ANNUAL_FEE_NGN=5000
CLUB_MONTHLY_FEE_NGN=15000
CLUB_QUARTERLY_FEE_NGN=42500
CLUB_BIANNUAL_FEE_NGN=80000
CLUB_ANNUAL_FEE_NGN=150000

# Optional: MCP-related configuration (if needed by MCP server)
MCP_BACKEND_URL=http://localhost:8000
ADMIN_EMAILS=["admin@admin.com"]
FRONTEND_URL=http://localhost:3000