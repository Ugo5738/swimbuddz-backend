"""add_challenge_series_fields

Phase B of the challenges revamp — turns standalone challenges into
ordered skill ladders. Three nullable columns on `club_challenges`:

  * series_slug — groups challenges into a ladder (e.g. "club-fundamentals")
  * series_order — step number within the ladder (1, 2, 3, ...)
  * requires_challenge_id — OPTIONAL hard-gating: members must have an
    approved badge for the prerequisite before submitting. Soft
    progression (no requires_challenge_id) is the default; admins
    opt INTO gating per challenge.

All three are nullable so existing standalone challenges keep working
unchanged.

Revision ID: 83d5ad494b80
Revises: 855e579b1d78
Create Date: 2026-05-10 19:26:33.859200
"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = '83d5ad494b80'
down_revision = '855e579b1d78'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column('club_challenges', sa.Column('series_slug', sa.String(), nullable=True))
    op.add_column('club_challenges', sa.Column('series_order', sa.Integer(), nullable=True))
    op.add_column('club_challenges', sa.Column('requires_challenge_id', sa.UUID(), nullable=True))
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column('club_challenges', 'requires_challenge_id')
    op.drop_column('club_challenges', 'series_order')
    op.drop_column('club_challenges', 'series_slug')
    # ### end Alembic commands ###
